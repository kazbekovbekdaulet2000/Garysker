import { LocaleDataModel } from "@core/models/local/locale.model";

const support: LocaleDataModel = {
  main_text: {
    header: "Помогите нам делать больше (каз)",
    about: "We'll get right to the point: we're asking you to help support (каз)"
  }
};

export default support;
